# Install everything with:  pip3 install -r requirements.txt

# --- Telegram bot (interface #1, the one we run this weekend) -------------
python-telegram-bot>=21,<24

# --- Web app (interface #2, optional) -------------------------------------
fastapi>=0.110
uvicorn>=0.27          # the little web server that runs FastAPI
python-multipart>=0.0.9  # lets FastAPI read submitted HTML forms

# --- Running the tests ----------------------------------------------------
pytest>=8
httpx>=0.27            # used by the tests to pretend to be a browser

# The game engine itself needs nothing beyond Python's own standard library:
# no dependency can break the maths in assignments.py.
